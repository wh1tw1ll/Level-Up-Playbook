// ── DOVA ARENA DASHBOARD — Redirect to external dashboard ──
// The DOVA dashboard lives at its own URL — this view redirects there.

function renderDovaDashboard() {
  window.location.href = 'https://dova-dashboard-ten.vercel.app/';
}